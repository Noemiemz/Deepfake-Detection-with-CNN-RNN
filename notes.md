### Choices made
- when there are two faces detected in the same frame, only the image with the largest surface is saved. This is to avoid saving images with a wrong label.
- started with only the dataset called `DeepFakeDetection`.